{
  id: recipe.id,
  title: recipe.title,
  cooktime: recipe.cooktime,
  image: recipe.image,
  ingredient: recipe.ingredient,
  direction: recipe.direction,
  tag: recipe.hashtags.map do |hashtag|
    {
      id: hashtag.id,
      tag: hashtag.tag,
    }
  end,
}
